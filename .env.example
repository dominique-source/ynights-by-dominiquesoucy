# Staff dashboard password (server-side only — never exposed to the client bundle).
# Falls back to "ynight2026" in development if unset. Set a real value in
# Vercel project settings before this ever ships to production.
STAFF_PASSWORD=

# Reserved for when Supabase is connected — not read anywhere yet.
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
